export default {
    API: {
        authenticate: `/users/authenticate`,
        affiliates: `/affiliates`,
        users: `/users`,
        resetPassword: `/users/passwdres`
    },
    STORAGE: {
        token: `kento`
    }
}
