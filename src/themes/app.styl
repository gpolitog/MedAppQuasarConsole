@import 'app.variables'

.button-container {
    margin: 10px 0;
}

.centered {        
    text-align: center;
}

.error-msg {
    font-size: 0.9rem;
    color: $negative;
}

.disabled-link {
  pointer-events: none;
  cursor: default;
}